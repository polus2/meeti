!!!
%html
  %head
    %title Meeti
    = stylesheet_link_tag :all
    = javascript_include_tag :defaults
    = csrf_meta_tag
  %body
    #world-wrapper
      #base-container
        #header-layer
          %a#logo_link{:href => root_path}
            %h1 Meeti
          = render "auth/header"
          = yield :header
        #content-layer
          #content-left-panel
            = yield
          #content-right-panel
            = yield :right
          .clearfix

